import PlaceholderPage from '../PlaceholderPage'

export default function CMSMenuList() {
  return (
    <PlaceholderPage
      title="CMS menus"
      description="React mirror of Vue /cp/cms/menus."
    />
  )
}
