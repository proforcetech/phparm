import PlaceholderPage from '../PlaceholderPage'

export default function PullRequestList() {
  return (
    <PlaceholderPage
      title="Inventory pull requests"
      description="React mirror of Vue /cp/inventory/pull-requests."
    />
  )
}
